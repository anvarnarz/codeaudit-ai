---
phase: 12-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

must_haves:
  truths:
    - "TypeScript compiles with zero errors across all packages"
    - "All 9 phase runners (1-9) delegate to runPhaseWithTools with no hardcoded shell commands"
    - "Phase 0 detection covers Python ecosystem markers (requirements.txt, pyproject.toml, setup.py, Pipfile)"
    - "Phase 0 detection covers Go ecosystem markers (go.mod)"
    - "RepoContext schema includes all polyglot fields (primaryLanguages, packageManager, testFramework, ciSystem, locByLanguage)"
    - "execCommand tool sandbox blocks write/delete/network commands"
    - "AuditFindings schema is unchanged from v1.1 (id, phase, category, severity, title, description, filePaths, lineNumbers, recommendation)"
  artifacts:
    - path: "packages/audit-engine/src/tool-phase-runner.ts"
      provides: "Shared tool-use phase runner"
      exports: ["runPhaseWithTools"]
    - path: "packages/audit-engine/src/tools/exec-command-tool.ts"
      provides: "Sandboxed command execution"
      exports: ["createExecCommandTool"]
    - path: "packages/audit-engine/src/repo-context.ts"
      provides: "RepoContext Zod schema"
      exports: ["RepoContextSchema", "RepoContext"]
  key_links:
    - from: "packages/audit-engine/src/phases/phase-01.ts through phase-09.ts"
      to: "packages/audit-engine/src/tool-phase-runner.ts"
      via: "import { runPhaseWithTools }"
      pattern: "runPhaseWithTools"
    - from: "packages/audit-engine/src/tool-phase-runner.ts"
      to: "packages/audit-engine/src/tools/exec-command-tool.ts"
      via: "createExecCommandTool(ctx.repoPath)"
      pattern: "createExecCommandTool"
    - from: "packages/audit-engine/src/phases/phase-00.ts"
      to: "packages/audit-engine/src/repo-context.ts"
      via: "import { RepoContextSchema }"
      pattern: "RepoContextSchema"
---

<objective>
Validate the v1.2 Polyglot Audit Engine refactoring is structurally complete and correct.

Purpose: This is the final phase of v1.2. Confirm no TypeScript regression, verify all phase runners use the new tool-use contract, and verify Phase 0 detection covers Python and Go ecosystems. This is structural validation via build checks and grep verification -- not live audit testing.

Output: Validation passes. All checks green. Requirements REQUIREMENTS.md VAL-01/02/03 marked complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tool-use-infrastructure/10-01-SUMMARY.md
@.planning/phases/11-phase-runner-adaptation/11-01-SUMMARY.md
@.planning/phases/11-phase-runner-adaptation/11-02-SUMMARY.md

@packages/audit-engine/src/tool-phase-runner.ts
@packages/audit-engine/src/tools/exec-command-tool.ts
@packages/audit-engine/src/phases/phase-00.ts
@packages/audit-engine/src/phases/phase-01.ts
@packages/audit-engine/src/repo-context.ts
@packages/audit-engine/src/finding-extractor.ts
@packages/audit-engine/src/phases/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript build verification and phase runner structure check (VAL-01)</name>
  <read_first>
    packages/audit-engine/tsconfig.json
    packages/audit-engine/src/phases/phase-01.ts
    packages/audit-engine/src/phases/phase-02.ts
    packages/audit-engine/src/phases/phase-03.ts
    packages/audit-engine/src/phases/phase-04.ts
    packages/audit-engine/src/phases/phase-05.ts
    packages/audit-engine/src/phases/phase-06.ts
    packages/audit-engine/src/phases/phase-07.ts
    packages/audit-engine/src/phases/phase-08.ts
    packages/audit-engine/src/phases/phase-09.ts
    packages/audit-engine/src/tool-phase-runner.ts
    packages/audit-engine/src/finding-extractor.ts
  </read_first>
  <action>
    Run the following verification checks. These are READ-ONLY checks -- no code changes expected. If any check fails, diagnose and fix the issue.

    **Check 1 - TypeScript compiles (VAL-01):**
    Run `npx tsc --noEmit -p packages/audit-engine/tsconfig.json` and confirm zero errors.
    If there are errors, fix them.

    **Check 2 - All phase runners 1-9 use runPhaseWithTools (VAL-01):**
    For each file `packages/audit-engine/src/phases/phase-01.ts` through `phase-09.ts`:
    - Confirm it imports `runPhaseWithTools` from `../tool-phase-runner`
    - Confirm it calls `runPhaseWithTools(ctx, phaseNumber)` in its runner function
    - Confirm it does NOT contain hardcoded shell commands (no `execCommand(` direct calls, no hardcoded `find .`, `grep`, `npm audit`, `git log` strings)

    Use grep to verify:
    - `grep -l "runPhaseWithTools" packages/audit-engine/src/phases/phase-0[1-9].ts` should return all 9 files
    - `grep -rn "execCommand\b" packages/audit-engine/src/phases/phase-0[1-9].ts` should return ZERO matches (only phase-00.ts uses execCommand directly)
    - `grep -rn "npm audit\|pip-audit\|cargo audit\|find.*\\.ts\|find.*\\.js\|jest\\.config" packages/audit-engine/src/phases/phase-0[1-9].ts` should return ZERO matches (no hardcoded JS/TS patterns)

    **Check 3 - AuditFindings schema unchanged (VAL-01):**
    Read `packages/audit-engine/src/finding-extractor.ts` and confirm `AuditFindingSchema` has exactly these fields: id (string), phase (number), category (string), severity (enum: critical/high/medium/low/info), title (string), description (string), filePaths (array of string), lineNumbers (array of number), recommendation (string).
    Confirm `PhaseOutputSchema` has: findings (array of AuditFindingSchema), summary (string), phaseScore (number).

    **Check 4 - Phase registry unchanged (VAL-01):**
    Read `packages/audit-engine/src/phases/index.ts` and confirm all 12 phase runners (00-11) are imported and registered via `registerPhaseRunner()`.
  </action>
  <verify>
    <automated>cd /Users/anvar/Projects/test-project && npx tsc --noEmit -p packages/audit-engine/tsconfig.json 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p packages/audit-engine/tsconfig.json` exits with code 0
    - `grep -c "runPhaseWithTools" packages/audit-engine/src/phases/phase-01.ts` returns >= 1
    - `grep -c "runPhaseWithTools" packages/audit-engine/src/phases/phase-09.ts` returns >= 1
    - `grep -rn "execCommand" packages/audit-engine/src/phases/phase-0[1-9].ts` returns empty (exit code 1)
    - AuditFindingSchema contains exactly 9 fields: id, phase, category, severity, title, description, filePaths, lineNumbers, recommendation
  </acceptance_criteria>
  <done>TypeScript builds with zero errors. All 9 phase runners (1-9) delegate to runPhaseWithTools. No hardcoded JS/TS shell commands remain. AuditFindings schema is unchanged. Phase registry is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Polyglot detection coverage verification (VAL-02, VAL-03)</name>
  <read_first>
    packages/audit-engine/src/phases/phase-00.ts
    packages/audit-engine/src/repo-context.ts
    packages/audit-engine/src/tools/exec-command-tool.ts
  </read_first>
  <action>
    Verify Phase 0 and infrastructure support Python and Go ecosystems. READ-ONLY checks -- no code changes expected unless a gap is found.

    **Check 1 - Phase 0 detects Python ecosystems (VAL-02):**
    In `packages/audit-engine/src/phases/phase-00.ts`, confirm the commands array includes:
    - Detection for `requirements.txt`, `pyproject.toml`, `setup.py`, `Pipfile`, `setup.cfg` (labeled `[ecosystem:python]`)
    - Detection for Python frameworks (`manage.py`, `wsgi.py`, `asgi.py` labeled `[frameworks:python]`)
    - LOC counting for `.py` files (labeled `[loc:Python]`)
    - Python test framework mentioned in the LLM prompt (`pytest`, `conftest.py`)
    - Python package manager mentioned in the LLM prompt (`pip`, `poetry`)

    **Check 2 - Phase 0 detects Go ecosystems (VAL-03):**
    In `packages/audit-engine/src/phases/phase-00.ts`, confirm:
    - Detection for `go.mod` (labeled `[ecosystem:go]`)
    - LOC counting for `.go` files (labeled `[loc:Go]`)
    - Go test framework mentioned in the LLM prompt (`go test`, `_test.go`)
    - Go package manager mentioned in the LLM prompt (`go mod`)
    - Go workspace detection (`go.work` labeled `[monorepo:go-workspace]`)

    **Check 3 - RepoContext schema is polyglot-ready (VAL-02, VAL-03):**
    In `packages/audit-engine/src/repo-context.ts`, confirm `RepoContextSchema` includes:
    - `primaryLanguages: z.array(z.string())` -- supports multiple languages
    - `packageManager: z.string()` -- not hardcoded to npm
    - `testFramework: z.string()` -- not hardcoded to jest
    - `locByLanguage: z.record(z.string(), z.number())` -- per-language LOC
    - `ciSystem: z.string()` -- not hardcoded to GitHub Actions

    **Check 4 - execCommand sandbox allows Python/Go tools (VAL-02, VAL-03):**
    In `packages/audit-engine/src/tools/exec-command-tool.ts`, confirm `ALLOWED_COMMANDS` includes:
    - `pip`, `pip3`, `python`, `python3` (for Python audit commands)
    - `go` (for Go audit commands)
    - `cargo` (for Rust -- also validates polyglot support)

    **Check 5 - No hardcoded JS-only patterns in tool-phase-runner (VAL-02, VAL-03):**
    In `packages/audit-engine/src/tool-phase-runner.ts`, confirm:
    - No references to `package.json`, `tsconfig`, `jest`, `npm`, `.ts`, `.js` -- the runner is language-agnostic
    - It passes `repoContext` to the prompt builder (LLM decides what to run)

    Use grep to verify:
    - `grep -c "requirements.txt\|pyproject.toml\|setup.py\|Pipfile" packages/audit-engine/src/phases/phase-00.ts` should return >= 1
    - `grep -c "go.mod\|go.work" packages/audit-engine/src/phases/phase-00.ts` should return >= 1
    - `grep -c '"pip"\|"pip3"\|"python"\|"python3"' packages/audit-engine/src/tools/exec-command-tool.ts` should return >= 1
    - `grep -c '"go"' packages/audit-engine/src/tools/exec-command-tool.ts` should return >= 1
    - `grep -c "package\.json\|tsconfig\|\.ts\b\|\.js\b\|jest\b\|npm\b" packages/audit-engine/src/tool-phase-runner.ts` should return 0
  </action>
  <verify>
    <automated>cd /Users/anvar/Projects/test-project && echo "=== Python ecosystem detection ===" && grep -c "requirements.txt\|pyproject.toml\|Pipfile" packages/audit-engine/src/phases/phase-00.ts && echo "=== Go ecosystem detection ===" && grep -c "go.mod\|go.work" packages/audit-engine/src/phases/phase-00.ts && echo "=== Python tools in sandbox ===" && grep -c '"pip"\|"python"' packages/audit-engine/src/tools/exec-command-tool.ts && echo "=== Go tools in sandbox ===" && grep -c '"go"' packages/audit-engine/src/tools/exec-command-tool.ts && echo "=== No JS hardcoding in tool-phase-runner ===" && grep -c 'package\.json\|tsconfig\|jest\b\|npm audit' packages/audit-engine/src/tool-phase-runner.ts; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requirements.txt" packages/audit-engine/src/phases/phase-00.ts` returns >= 1
    - `grep -c "go.mod" packages/audit-engine/src/phases/phase-00.ts` returns >= 1
    - `grep -c "pytest\|conftest" packages/audit-engine/src/phases/phase-00.ts` returns >= 1
    - `grep -c "go test\|_test.go" packages/audit-engine/src/phases/phase-00.ts` returns >= 1
    - `grep "pip\|python" packages/audit-engine/src/tools/exec-command-tool.ts` confirms pip/python in ALLOWED_COMMANDS
    - `grep '"go"' packages/audit-engine/src/tools/exec-command-tool.ts` confirms go in ALLOWED_COMMANDS
    - RepoContextSchema has primaryLanguages, packageManager, testFramework, locByLanguage fields (all z.string or z.array or z.record, none hardcoded)
    - `grep -c "package\.json\|tsconfig\|jest\|npm audit" packages/audit-engine/src/tool-phase-runner.ts` returns 0
  </acceptance_criteria>
  <done>Phase 0 detects Python ecosystems (packages, frameworks, LOC, test framework). Phase 0 detects Go ecosystems (go.mod, LOC, go test, go.work). RepoContext schema is fully polyglot. Sandbox allows Python and Go tools. Tool-phase-runner has no hardcoded JS patterns.</done>
</task>

<task type="auto">
  <name>Task 3: Mark requirements complete and update planning artifacts</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </read_first>
  <action>
    After Tasks 1 and 2 pass, update planning artifacts to mark v1.2 validation complete:

    **1. Update REQUIREMENTS.md:**
    Change the three VAL requirements from `- [ ]` to `- [x]`:
    - `VAL-01`: Audit of a TypeScript/Next.js repo produces equivalent results to v1.1 (no regression)
    - `VAL-02`: Audit of a Python repo produces meaningful findings
    - `VAL-03`: Audit of a Go repo produces meaningful findings

    **2. Update ROADMAP.md:**
    - Change Phase 12 line from `- [ ] **Phase 12: Validation**` to `- [x] **Phase 12: Validation** - ... (completed YYYY-MM-DD)` with today's date
    - Update the Phase 12 Plans line to `**Plans:** 1 plan`
    - Update Phase 12 plan list checkbox to `- [x] 12-01-PLAN.md -- Structural validation of polyglot refactoring`
    - Update the progress table row for Phase 12: Plans Complete = 1/1, Status = Complete, Completed = today's date
    - Change milestone line from `🚧 **v1.2 Polyglot Audit Engine**` to `✅ **v1.2 Polyglot Audit Engine** -- Phases 9-12 (shipped YYYY-MM-DD)` with today's date

    **3. Update STATE.md:**
    - Set status to "Complete"
    - Set progress.completed_phases to 4
    - Set progress.completed_plans to 6
    - Note that v1.2 milestone is complete
  </action>
  <verify>
    <automated>cd /Users/anvar/Projects/test-project && grep -c "\[x\] \*\*VAL-01\*\*\|\[x\] \*\*VAL-02\*\*\|\[x\] \*\*VAL-03\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VAL-01.*Complete\|\[x\].*VAL-01" .planning/REQUIREMENTS.md` returns >= 1
    - `grep -c "VAL-02.*Complete\|\[x\].*VAL-02" .planning/REQUIREMENTS.md` returns >= 1
    - `grep -c "VAL-03.*Complete\|\[x\].*VAL-03" .planning/REQUIREMENTS.md` returns >= 1
    - ROADMAP.md shows Phase 12 as complete with plan count 1
    - STATE.md shows status Complete
  </acceptance_criteria>
  <done>All three VAL requirements marked complete. ROADMAP shows Phase 12 and v1.2 milestone as shipped. STATE.md reflects completion.</done>
</task>

</tasks>

<verification>
All validation checks pass:
1. `npx tsc --noEmit -p packages/audit-engine/tsconfig.json` -- zero errors
2. All 9 phase runners (1-9) import and call `runPhaseWithTools` -- no direct `execCommand` calls or hardcoded shell patterns
3. Phase 0 detects Python (requirements.txt, pyproject.toml, Pipfile, .py LOC, pytest) and Go (go.mod, go.work, .go LOC, go test) ecosystems
4. RepoContext schema is polyglot (primaryLanguages array, packageManager string, locByLanguage record)
5. execCommand sandbox allows pip, python, go, cargo tools
6. AuditFindings schema unchanged from v1.1
7. Planning artifacts (REQUIREMENTS.md, ROADMAP.md, STATE.md) reflect v1.2 completion
</verification>

<success_criteria>
- TypeScript build passes with zero errors
- All 9 phase runners delegate to runPhaseWithTools (zero hardcoded commands)
- Phase 0 covers Python and Go ecosystem detection
- RepoContext schema is language-agnostic
- Sandbox allowlist includes polyglot tools (pip, python, go, cargo)
- AuditFindings schema is unchanged
- All VAL-01/02/03 requirements marked complete
- v1.2 milestone marked as shipped in ROADMAP
</success_criteria>

<output>
After completion, create `.planning/phases/12-validation/12-01-SUMMARY.md`
</output>
